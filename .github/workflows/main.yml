name: Create a Docker image to place it on Azure Container repo
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: azure/docker-login@v1
        with:
          login-server: videocontainer.azurecr.io
          username: videocontainer
          password: 0suBumOYPwLgJtCxTgMFxQgdUgANeISQU2HdX7j/wD+ACRDsLlSu
      - run: |
          docker build . -t videocontainer.azurecr.io/matt95:${{ github.sha }}
          docker push videocontainer.azurecr.io/matt95:${{ github.sha }}