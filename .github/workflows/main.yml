name: Create a Docker image to place it on Azure Container repo
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: azure/docker-login@v1
        with:
          login-server: containervideostuff.azurecr.io
          username: containervideostuff
          password: irdx22FzFcU3TgD8D22Cv5mjgFiYlNlDtB+5irrNp++ACRDmzeat
      - run: |
          docker build . -t containervideostuff.azurecr.io/deployments:${{ github.sha }}
          docker push containervideostuff.azurecr.io/deployments:${{ github.sha }}